{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "setup": "pnpm install --prefix frontend && pnpm install --prefix backend",
    "build": "pnpm install && pnpm --prefix frontend run build",
    "start": "node backend/index.js",
    "render-build": "pnpm run setup && pnpm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "pnpm": "^10.11.0"
  }
}
